# OpenRouter API Key
# Get your API key from https://openrouter.ai/
OPENROUTER_API_KEY=your_api_key_here

# Default settings
DEFAULT_CONTEXT_SIZE=3
DEFAULT_MODEL=google/gemini-2.0-pro-exp-02-05:free
DEFAULT_LANGUAGE=Korean
API_URL=https://openrouter.ai/api/v1/chat/completions

# Additional Instructions (set to "default" to use the default instructions or provide your own)
# Each instruction should be on a separate line with INSTRUCTION_1, INSTRUCTION_2, etc.
INSTRUCTION_1=Preserve Markdown/LaTeX syntax. Do not alter any formatting symbols (e.g., `**bold**`, `_italics_`, `\\LaTeX` commands, headers, links, etc.).
INSTRUCTION_2=No summarization. Provide a direct translation of the paragraph without omission or addition.
INSTRUCTION_3=Retain paragraph structure. Don't merge or split sentences if not necessary.
INSTRUCTION_4=Keep academic or technical terms in English (even if the source text is in another language).
# Add more instructions as needed: INSTRUCTION_5, INSTRUCTION_6, etc. 